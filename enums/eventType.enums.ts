/**
 * EventType Enum
 * 
 * This enum defines the various categories of events supported by the application.
 * It provides a standardized set of event types that can be used for categorization,
 * filtering, and reporting purposes throughout the application.
 * 
 * @enum {string}
 */
export enum EventType {
    SOCIAL = 'SOCIAL',
    EDUCATION = 'EDUCATION',
    BUSINESS = 'BUSINESS',
    ENTERTAINMENT = 'ENTERTAINMENT',
    OTHER = 'OTHER',
}